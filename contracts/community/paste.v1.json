{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://openvibe.network/contracts/community/paste.v1.json",
  "title": "CommunityPaste",
  "description": "community.paste@1: one paste as OpenVibe.Community's /api/pastes answers it (server/pastes/service.js shape: Media's pastePublic shape plus the author projection). user_id carries the owner's subject id (Community has no numeric user ids), so a falsy user_id still means anonymous. Times are SQLite UTC text. In lists, content is a 300-character preview (null for a screenshot).",
  "type": "object",
  "required": ["id", "app_id", "slug", "user_id", "owner_subject", "origin", "type", "title", "visibility", "url", "raw_url", "created_at", "is_owner"],
  "properties": {
    "id": { "type": "integer" },
    "app_id": { "const": "community" },
    "slug": { "type": "string" },
    "user_id": { "type": ["string", "null"], "description": "The owner's subject (usr_…), or null for an anonymous paste." },
    "owner_subject": { "type": ["string", "null"] },
    "origin": { "type": "string", "description": "user, or ai for a paste an AI service wrote." },
    "type": { "enum": ["paste", "screenshot"] },
    "title": { "type": "string" },
    "content": { "type": ["string", "null"] },
    "language": { "type": ["string", "null"] },
    "visibility": { "enum": ["public", "unlisted", "private"] },
    "stream_id": { "type": ["integer", "null"], "description": "The Live stream it was made during, when its stream_ref is one." },
    "stream_ref": { "type": ["object", "null"], "description": "An EntityRef of where it was made (a Live stream, or a service's own source)." },
    "screenshot_url": { "type": ["string", "null"] },
    "metadata": { "description": "Structured metadata a service attached (AI moments), or a screenshot's upload details." },
    "burn_after_read": { "type": "boolean" },
    "forked_from": { "type": ["integer", "null"] },
    "pinned": { "type": "boolean" },
    "views": { "type": "integer", "minimum": 0 },
    "unique_views": { "type": "integer", "minimum": 0 },
    "copies": { "type": "integer", "minimum": 0 },
    "likes": { "type": "integer", "minimum": 0 },
    "is_nsfw": { "type": "boolean" },
    "ai_summary": { "type": ["string", "null"] },
    "ai_tags": { "type": ["string", "null"], "description": "JSON text as the AI pass wrote it." },
    "ai_analyzed_at": { "type": ["string", "null"] },
    "revision": { "type": "integer" },
    "url": { "type": "string", "description": "/p/<slug>" },
    "raw_url": { "type": "string", "description": "/p/<slug>/raw" },
    "created_at": { "type": "string" },
    "updated_at": { "type": ["string", "null"] },
    "is_owner": { "type": "boolean", "description": "Whether the caller owns it." },
    "username": { "type": ["string", "null"] },
    "display_name": { "type": ["string", "null"], "description": "OpenVibe AI for an AI paste." },
    "avatar_url": { "type": ["string", "null"] },
    "profile_color": { "type": ["string", "null"] }
  }
}
