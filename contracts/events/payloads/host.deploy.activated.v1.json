{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://openvibe.network/contracts/events/payloads/host.deploy.activated.v1.json",
  "title": "HostDeployActivatedPayload",
  "description": "host.deploy.activated v1 (OpenVibe.Host server/domain/deploys.js switchPointer, Stage B tenant hosting). A tenant site's active deploy changed, written into Host's event outbox in the transaction that moves the pointer (activate or rollback). Envelope: subject { type: deploy, id: <deploy id> }, visibility internal, priority low, actor the person or app that switched it. A network service's release going live is host.release.published, not this.",
  "type": "object",
  "required": [
    "project_id",
    "site_id",
    "site",
    "deploy_id",
    "previous_deploy_id",
    "rollback"
  ],
  "additionalProperties": false,
  "properties": {
    "project_id": {
      "type": "string",
      "pattern": "^prj_[0-9A-HJKMNP-TV-Z]{26}$"
    },
    "site_id": {
      "type": "string",
      "pattern": "^site_[0-9A-HJKMNP-TV-Z]{26}$"
    },
    "site": {
      "type": "string",
      "pattern": "^[a-z0-9](?:[a-z0-9]|-(?=[a-z0-9])){2,39}$",
      "description": "The site's name (<site>.openvibe.host)."
    },
    "deploy_id": {
      "type": "string",
      "pattern": "^dpl_[0-9A-HJKMNP-TV-Z]{26}$"
    },
    "previous_deploy_id": {
      "type": [
        "string",
        "null"
      ],
      "pattern": "^dpl_[0-9A-HJKMNP-TV-Z]{26}$",
      "description": "The deploy that served before; null for the site's first activation."
    },
    "rollback": {
      "type": "boolean",
      "description": "true when the switch was a rollback."
    }
  }
}
