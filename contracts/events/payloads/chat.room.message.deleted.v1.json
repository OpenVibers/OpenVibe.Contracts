{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://openvibe.network/contracts/events/payloads/chat.room.message.deleted.v1.json",
  "title": "ChatRoomMessageDeletedPayload",
  "description": "chat.room.message.deleted v1 (OpenVibe.Chat server/rooms/rooms.js, WS-I task 9). Messages of a public room were deleted (by the author, a room mod or chat staff), or the room turned private or was archived, so what it had published must stop being replayable. Envelope: subject { type: chat_room_message, id: <first id> }, visibility public, actor service:chat. Carries ids only; payload.redacts makes OpenVibe.Events tombstone the chat.room.message.created of each id.",
  "type": "object",
  "required": ["message_ids", "redacts"],
  "additionalProperties": false,
  "properties": {
    "room": { "type": "string", "maxLength": 64, "description": "The room's slug." },
    "message_ids": { "type": "array", "minItems": 1, "maxItems": 500, "uniqueItems": true, "items": { "type": "integer", "minimum": 1 } },
    "redacts": { "$ref": "../redaction-directive.v1.json", "description": "{ subject_type: chat_room_message, subject_ids: message_ids as strings }." }
  }
}
