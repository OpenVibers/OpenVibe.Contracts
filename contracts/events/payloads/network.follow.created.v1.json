{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://openvibe.network/contracts/events/payloads/network.follow.created.v1.json",
  "title": "NetworkFollowCreatedPayload",
  "description": "network.follow.created v1 (OpenVibe.Network; roadmap WS-E task 4, ADR-030). A person followed something: today a Live channel, whose target id is the channel owner's subject. Follows are the network's, kept by Network and keyed by subjects; products such as Live keep a projection they can rebuild. Written to Network's outbox in the transaction that stores the follow. A consumer applies the latest revision per (follower, target) and ignores an older one. Following again what is already followed changes nothing and sends nothing. Envelope: subject { type: user, id: <follower> }, visibility subject, actor the follower.",
  "type": "object",
  "required": ["follower", "target_type", "target_id", "revision"],
  "additionalProperties": false,
  "properties": {
    "follower": { "type": "string", "pattern": "^usr_[0-9A-HJKMNP-TV-Z]{26}$", "description": "Who follows." },
    "target_type": { "enum": ["channel"], "description": "What kind of thing is followed. channel: a Live channel, named by its owner's subject." },
    "target_id": { "type": "string", "pattern": "^usr_[0-9A-HJKMNP-TV-Z]{26}$", "description": "The followed target: for a channel, the channel owner's subject." },
    "notify_email": { "type": "boolean" },
    "notify_push": { "type": "boolean" },
    "revision": { "type": "integer", "minimum": 1, "maximum": 9007199254740991, "description": "Grows with every change of this pair." },
    "at": { "type": "string", "format": "date-time" }
  }
}
