{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://openvibe.network/contracts/identity/realtime-ticket-claims.v1.json",
  "title": "RealtimeTicketClaims",
  "description": "Claims of a realtime ticket (ADR-005 amendment 2; roadmap WS-E task 3, WS-F task 1): a two-minute RS256 JWT that OpenVibe.Network signs for the signed-in person (POST /api/v1/realtime/ticket) so a browser on any OpenVibe site can open Events' /realtime/stream?ticket=... as that person. A browser cannot put a header on an EventSource, and the ov_token cookie of events.openvibe.network is third-party on every other site, so the ticket travels in the URL; that is why it is short-lived, single-purpose and single-use. It is never a session token, by three independent rules: its issuer is Network's issuer followed by /realtime (every service checks the issuer of a session token), it carries typ (services refuse a session token that has one, as for FedCM assertions), and its only audience is openvibe.events. Events accepts it only as ?ticket= on /realtime/stream, verifies the signature with Network's key, iss, aud, typ, purpose and expiry (lifetime at most 300 s), refuses a jti it has already seen while the ticket is valid, and never logs it. The connection then sees what the person's session would: public events and subject events addressed to `sub`.",
  "type": "object",
  "required": [
    "iss",
    "sub",
    "aud",
    "typ",
    "purpose",
    "iat",
    "exp",
    "jti"
  ],
  "additionalProperties": false,
  "properties": {
    "iss": {
      "type": "string",
      "format": "uri",
      "pattern": "^https?://[^/?#]+/realtime$",
      "description": "Network's issuer followed by /realtime (https://openvibe.network/realtime in production): not the issuer of a session."
    },
    "sub": {
      "type": "string",
      "pattern": "^usr_[0-9A-HJKMNP-TV-Z]{26}$",
      "description": "The person's subject id; subject-visibility events for this subject reach the connection."
    },
    "aud": {
      "type": "array",
      "minItems": 1,
      "maxItems": 1,
      "items": {
        "const": "openvibe.events"
      }
    },
    "typ": {
      "const": "realtime",
      "description": "Token class. A service that finds typ on a Network token does not treat it as a session."
    },
    "purpose": {
      "const": "realtime",
      "description": "Single purpose: opening one realtime stream."
    },
    "iat": {
      "type": "integer",
      "minimum": 0
    },
    "exp": {
      "type": "integer",
      "minimum": 0,
      "description": "iat + 120 as Network mints it; Events refuses a lifetime over 300 s."
    },
    "jti": {
      "type": "string",
      "pattern": "^rtk_[0-9a-f]{24}$",
      "description": "Unique per ticket; Events accepts each once."
    }
  }
}
