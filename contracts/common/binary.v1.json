{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://openvibe.network/contracts/common/binary.v1.json",
  "title": "Binary",
  "description": "common.binary@1: the response is the resource's bytes, not JSON — Content-Type names the media type, Content-Length (or Range with 206) the size. An error is still an application/problem+json body. A declaration for capabilities that serve files (roadmap WS-C task 4); there is nothing to validate but the headers.",
  "type": "string",
  "contentEncoding": "binary"
}
