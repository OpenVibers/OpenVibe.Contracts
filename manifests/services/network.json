{
  "version": "0.3.0",
  "contractRanges": {
    "openvibe-contracts": ">=0.2.0 <1.0.0"
  },
  "id": "network",
  "name": "OpenVibe.Network",
  "status": "stable",
  "repository": "OpenVibers/OpenVibe.Network",
  "domains": [
    "openvibe.network",
    "auth.openvibe.network",
    "api.openvibe.network",
    "admin.openvibe.network",
    "my.openvibe.network",
    "themes.openvibe.network"
  ],
  "publicOrigin": "https://openvibe.network",
  "health": "/api/health",
  "ready": "/api/ready",
  "internalOrigin": "http://127.0.0.1:4000",
  "exposure": {
    "state": "live",
    "publicSite": "service"
  },
  "site": {
    "name": "Network",
    "icon": "network",
    "tagline": "Account and themes",
    "what": "The account behind every site: one sign-in, themes, notifications, history and linked services.",
    "legalProfile": "account",
    "position": 5
  },
  "capabilities": [
    "identity.subject.resolve",
    "mods.grant.manage",
    "network.blocks.read",
    "network.coins.credit",
    "network.coins.debit",
    "network.coins.transfer",
    "network.follows.read",
    "network.integration.github.read",
    "network.modules.read",
    "network.modules.write",
    "network.notifications.push",
    "network.operator.alert",
    "network.project.manage",
    "network.project.read",
    "network.staff.read"
  ],
  "eventsProduced": [
    "network.app.created",
    "network.app.revoked",
    "network.block.changed",
    "network.credential.revoked",
    "network.credential.rotated",
    "network.follow.created",
    "network.follow.deleted",
    "network.grant.changed",
    "network.module.updated",
    "network.notification.created",
    "network.principal_grant.changed",
    "network.user.token_valid_after",
    "network.user.updated"
  ],
  "eventsConsumed": [
    "billing.staff.action",
    "blog.moderation.action",
    "chat.moderation.action",
    "codes.moderation.action",
    "community.moderation.action",
    "coupons.moderation.action",
    "deals.moderation.action",
    "deals.watch.matched",
    "events.usage.recorded",
    "games.moderation.action",
    "live.moderation.action",
    "live.stream.started",
    "media.moderation.action",
    "news.moderation.action",
    "reviews.moderation.action",
    "tips.interaction.moderated",
    "tools.moderation.action",
    "tools.usage.recorded",
    "trade.alert.triggered",
    "trade.moderation.action",
    "wiki.moderation.action"
  ],
  "namespacesOwned": [
    "identity.*",
    "network.*",
    "theme.*",
    "notification.*"
  ],
  "lifecycle": {
    "liveness": {
      "endpoint": "/api/health",
      "means": "the process is up and answers HTTP; it checks nothing else (the database and dependencies are what ready checks)"
    },
    "shutdown": {
      "signal": "SIGTERM",
      "deadlineSeconds": 10,
      "drains": [
        "maintenance timers (notifications, email queue, retired-module sweep, session cleanup), the analytics prune, the ecosystem, deploy-drift and library-tag pollers, the frame refreshes and the profile-event and grant-expiry timers stopped",
        "stops taking connections; idle keep-alive connections closed; requests in flight answered with Connection: close and allowed to finish (8 s at most, then cut)",
        "the developer, profile and grant event relay's send in progress awaited (unsent rows stay in the outbox)",
        "analytics flushed and closed; network.db closed; exit 0"
      ],
      "note": "server/graceful.js; a stop that passes 10 s exits 1. Only the Discord link's in-memory state sweep ends with the process; the email queue and the outboxes resume from the database on the next start"
    },
    "startupRecovery": {
      "resumes": [
        {
          "kind": "outbox",
          "what": "the developer-app, profile and grant event outboxes (developer/event-relay.js, identity/profile-events.js) send what is unsent"
        },
        {
          "kind": "jobs",
          "what": "the email queue is processed again every 2 minutes; the retired-owner module sweep runs 5 minutes after boot"
        },
        {
          "kind": "state",
          "what": "the ecosystem registry polls every service's readiness and release again; deploy drift and library tags refresh"
        },
        {
          "kind": "consumer",
          "what": "OpenVibe.Events redelivers the moderation, staff and module events it consumes that were pending during the stop (at least once, deduped by event_id)"
        }
      ]
    },
    "rollback": {
      "conditions": [
        "automatic: ovhost deploy puts the previous sha back and restarts again when the ready URL does not answer 2xx within ready.timeoutSeconds after the restart (exit 3)",
        "manual: ovhost rollback <service> --to <sha>"
      ],
      "window": "automatic only while ovhost waits for readiness after the restart (30 s in host.example.json); manual afterwards, as long as no blocker applies",
      "blockers": [
        "chrome_cache/chrome_hits were renamed to frame_cache/frame_hits (2026-09-24): a release from before that starts with empty caches",
        "tool_domains was rebuilt once to allow the mirror role: a release from before it does not know mirror rows"
      ]
    },
    "contracts": {
      "range": ">=0.2.0 <1.0.0"
    },
    "leases": {
      "none": "no claims: one process owns network.db (better-sqlite3, single writer); a stream-live announcement is claimed per streamer with a cooldown and a daily cap in the same database"
    }
  }
}
