{
  "id": "billing.intent.create",
  "version": "1.0",
  "owner": "billing",
  "status": "active",
  "visibility": "internal",
  "description": "Start a checkout: create a payment intent priced by Billing (never by the client) for a Vibes purchase or a channel subscription, and get the provider checkout URL or PowerChat app_ref. Also reads intents and captures approved PayPal orders.",
  "permissions": [
    "billing:intents"
  ],
  "resourceConstraints": [
    "owner"
  ],
  "quotaClass": "ledger-write",
  "events": [],
  "implementedBy": [
    "POST /api/v1/intents",
    "GET /api/v1/intents/:id",
    "POST /api/v1/intents/:id/capture"
  ],
  "inputSchema": "billing.intent-request@1",
  "outputSchema": "billing.intent-result@1"
}
